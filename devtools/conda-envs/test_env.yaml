name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - jinja2
  - pathos

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - codecov
    - pytraj

